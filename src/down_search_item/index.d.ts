import type { search_params_T } from '../_types'
/**
 * Returns a `down_search_item` function, which sets `idx` to the next value
 */
export declare function down_search_item<
	I extends unknown = unknown
>({ a_, idx_, }:search_params_T<I>):void
